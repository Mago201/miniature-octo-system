# XAU_Scalper_Aggressive (MT5)

Агрессивный скальпер по золоту (XAUUSD) для MetaTrader 5.

## Стратегия

- Таймфрейм: **M1** (рекомендован) или M5.
- Вход в **BUY**: цена ≤ нижней Bollinger Band **И** RSI ≤ уровня перепроданности.
- Вход в **SELL**: цена ≥ верхней Bollinger Band **И** RSI ≥ уровня перекупленности.
- Опциональный фильтр тренда по EMA (по умолчанию **выключен** — это агрессивный режим).
- Короткий тейк-профит, без стоп-лосса по умолчанию.
- Безубыток + трейлинг-стоп.
- **Мартингейл**: после убытка следующий лот умножается на множитель (с потолком).
- Возможность держать сетку из нескольких позиций одновременно.
- Жёсткий фильтр спреда (для золота критичен).
- Фильтр торговых часов и день-недели.
- Аварийный «капитальный» стоп: если просадка эквити превысит порог, всё закрывается и советник снимается с графика.

## ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ О РИСКАХ

Параметры по умолчанию рассчитаны на **максимально агрессивный** режим:
- стоп-лосс отключён;
- включён мартингейл (×2 после каждого убытка);
- разрешено держать до 3 позиций в одну сторону;
- сигналы анализируются на каждом тике, не только на закрытии бара.

Такая конфигурация способна **слить депозит за одну неудачную серию**.
Запускайте сначала **только на демо-счёте**. Для реальных денег уменьшите
`InpMartingaleMult` до 1.0 (выкл. мартингейл), задайте `InpStopLossPts > 0`
и оставьте `InpMaxPositions = 1`.

## Установка

1. Скопируйте `XAU_Scalper_Aggressive.mq5` в каталог терминала
   `MQL5/Experts/` (Файл → Открыть каталог данных в MT5).
2. В MetaEditor: `F7` для компиляции — должен пройти без ошибок.
3. Перезапустите MT5 (или нажмите `Refresh` в навигаторе советников).
4. Откройте график **XAUUSD M1**, перетащите советник на график.
5. Во вкладке «Общие» отметьте «Разрешить алгоритмическую торговлю».
6. Нажмите кнопку «Алготрейдинг» в верхней панели MT5.

## Ключевые параметры

| Параметр | По умолчанию | Описание |
|---|---|---|
| `InpMagic` | `20260519` | Magic-номер. Меняйте, если запускаете несколько копий. |
| `InpLotMode` | `LOT_MARTINGALE` | `LOT_FIXED` / `LOT_RISK_PCT` / `LOT_MARTINGALE`. |
| `InpLotFixed` | `0.01` | Базовый лот. |
| `InpMartingaleMult` | `2.0` | Множитель лота после убытка. |
| `InpMartingaleMaxLot` | `1.00` | Потолок лота при мартингейле. |
| `InpMaxLossesInRow` | `6` | После N убытков подряд — сброс к базовому лоту. |
| `InpTakeProfitPts` | `80` | TP в пунктах цены (`_Point`). Для XAUUSD с 2 знаками 80 пунктов = $0.80. |
| `InpStopLossPts` | `0` | SL в пунктах. `0` — без стопа (опасно). |
| `InpUseTrailing` | `true` | Трейлинг включён. |
| `InpUseBreakeven` | `true` | Перевод в БУ. |
| `InpMaxPositions` | `3` | Лимит одновременных позиций (1 — без сетки). |
| `InpMaxSpreadPts` | `35` | Жёсткий фильтр спреда. Для XAUUSD у нормального брокера типично 20–30. |
| `InpUseTimeFilter` | `true` | Торговать только в активные часы. |
| `InpHourStart` / `InpHourEnd` | `8` / `21` | Часы по серверному времени брокера. |
| `InpRSIPeriod` | `7` | Короткий RSI для скальпа. |
| `InpRSIOversold` / `InpRSIOverbought` | `25` / `75` | Уровни. |
| `InpBBPeriod` / `InpBBDeviation` | `20` / `2.0` | Bollinger Bands. |
| `InpUseTrendFilter` / `InpEMAPeriod` | `false` / `50` | Опциональный фильтр тренда. |
| `InpUseEquityStop` / `InpEquityStopPct` | `true` / `25%` | Аварийный стоп — закрыть всё при просадке эквити. |

## Рекомендации по тестированию

1. В тестере стратегий выберите символ XAUUSD, период M1.
2. Включите режим **Каждый тик на основе реальных тиков** (Every tick based on real ticks) — для скальпера принципиально.
3. Период — минимум 6 месяцев, лучше год, с разными режимами рынка.
4. Стартовый депозит — соразмерный реальному (например, $1000).
5. Обращайте внимание не только на итоговую прибыль, но и на:
   - **максимальную просадку** в %;
   - **самую длинную серию убытков** (актуально для мартингейла);
   - количество сделок при разных уровнях `InpMaxSpreadPts` — слишком высокий уровень даст много мусорных входов.

## Более «безопасный» пресет (если решите крутить вживую)

```
InpLotMode          = LOT_FIXED
InpLotFixed         = 0.01
InpStopLossPts      = 150
InpTakeProfitPts    = 80
InpMaxPositions     = 1
InpMaxSpreadPts     = 25
InpUseTrendFilter   = true
InpAllowOnNewBarOnly= true
InpUseEquityStop    = true
InpEquityStopPct    = 10.0
```

С таким набором EA становится обычным трендовым скальпером с положительным
матожиданием по входам в перепроданность/перекупленность.

## Лицензия

Учебный/демонстрационный код. Используйте на свой риск.
